# 发布身份与镜像晋升历史

从 0.11.5 起，项目只保留一套正式发布身份；原优化镜像已经完成晋升并替换旧源码目录。以下配置是当前唯一有效的默认构建配置：

```text
包名：fengyue-link
appId：cc.aiero.fengyue.link
NSIS GUID：63340c0b-8899-4ec0-9aa3-5a80da4ed183
产品/用户数据目录：风月联机工具
桌面快捷方式：风月联机工具
输出目录：release
卸载：保留正式版用户数据
```

构建命令：

```powershell
npm run pack:win
```

安装包以相同正式版身份覆盖升级现有程序，并继续读取正式版账号、Cookie 和本地配置。不得重新引入第二套活动发布身份；0.11.1 至 0.11.4 的以下内容只作为当时镜像隔离、晋升和验收的历史记录。

## 历史验收记录

## 0.11.1 验收记录（2026-09-03）

- 真实执行镜像静默安装、创建镜像数据标记、静默卸载和重新安装；卸载后安装目录、桌面快捷方式及镜像用户数据三者均不存在。
- 卸载前后，正式版桌面快捷方式存在性及 SHA-256 不变，正式版用户数据目录保持存在。
- 重装后的桌面快捷方式为 `C:\Users\sheliwithoutX\Desktop\风月联机工具优化镜像.lnk`，目标位于独立安装目录 `C:\Users\sheliwithoutX\AppData\Local\Programs\fengyue-link-optimized-mirror`；已验证启动、创建窗口并正常退出。
- 镜像安装包 SHA-256：`1206BDEB8C78154F1AE4990CDC7C286D9C8911BFA4A492C4920A02AA49F4877B`。
- 正式替换安装包 SHA-256：`8381B594765421545F869BA0394520CFB6B6A2E254ABF2A6AA00C2AFE468C9F9`。该包只完成构建与元数据核验，没有安装。

## 0.11.2 正式晋升记录（2026-09-03）

- 已关闭并卸载 0.11.1 优化镜像，清除其安装目录、Electron 用户数据、桌面/开始菜单快捷方式和卸载注册项。
- 原“正式版”桌面入口是指向 `D:\风月联机工具\启动桌面版.ps1` 的源码启动快捷方式；已只移除该快捷方式，原版源码目录保持不变。
- 已安装 0.11.2 正式替换包。桌面快捷方式为 `C:\Users\sheliwithoutX\Desktop\风月联机工具.lnk`，目标为 `C:\Users\sheliwithoutX\AppData\Local\Programs\fengyue-link\风月联机工具.exe`，卸载 GUID 为 `63340c0b-8899-4ec0-9aa3-5a80da4ed183`。
- 正式版用户数据目录 `%APPDATA%\风月联机工具` 在替换过程中保持存在；安装后使用临时隔离数据目录完成启动、正式产品名窗口和正常退出验证，临时目录随后删除。
- 已删除 0.11.0/0.11.1 旧安装包及 blockmap，只保留当前 0.11.2 交付产物。
- 镜像安装包 SHA-256：`3C71893E67CAB83E143B12522FDACD2D6E17FBC0E68904705464EF7C5761D8D4`。
- 已实装的正式替换安装包 SHA-256：`BE3AF1AE0A64649D530B9E2CFF89FDC913E1318574920F36537E41304291E95F`。

## 0.11.3 账号实例修复记录（2026-09-03）

- 确认 0.11.2 打包程序将 ASAR 虚拟路径作为 Windows 子进程工作目录，实际复现 `spawn ... ENOENT`。
- 打包模式改用正式可执行文件所在的真实目录，开发模式仍使用源码根目录；回归测试同时固定命令、参数、工作目录和实例名称清洗结果。
- 已覆盖安装正式版 0.11.3，正式用户数据保持存在。
- 使用已安装的正式可执行文件和正式安装目录，同时启动 `account-qa-a`、`account-qa-b` 两个隔离账号实例；两个窗口标题及进程均独立，随后都正常退出，临时测试数据已删除。
- 镜像安装包 SHA-256：`0EF092994822916C61DFA15EC7412C11665B3D9B4DAFB1B86D562244ECD04E00`。
- 已实装的正式替换安装包 SHA-256：`4D7915683003DE9247DEE16CE33E58BA129CE19F6301603EA8F83B949292AACE`。

## 0.11.4 刷新与插件审计记录（2026-09-03）

- 修复刷新、删除和角色删除仍使用网页确认框而可能被原生作品视图遮挡的问题；统一改用窗口级确认框。
- 刷新/编辑上一条回复统一经过输出插件栈，保留输入插件运行记录并替换旧输出记录；平台按钮支持位于回复节点外侧的消息行。
- 修复退出房间期间旧轮询访问空房间状态，并确保刷新失败或房间退出会解除独立视角临时展示锁；完成全部既有插件审计、172 项测试、沙箱验证和离线桌面验证。
- 打包器复用本地已核对为 44.0.0 的 Electron 运行时，移除重复下载对构建网络的依赖。
- 已覆盖安装正式版 0.11.4，正式用户数据与正式桌面快捷方式保持存在；镜像安装目录、镜像用户数据、镜像快捷方式和镜像卸载项均不存在。
- 安装后正式程序文件版本、ASAR 清单版本和卸载项版本均为 0.11.4，并已成功打开窗口“风月联机工具 · default”。
- 已删除 0.11.3 的镜像与正式替换安装包及 blockmap，只保留 0.11.4 交付产物。
- 镜像安装包 SHA-256：`B9195374C384CAC8AC96D2B259EA4C2BF013F948A8F967806BD4FB658E500102`。
- 已实装的正式替换安装包 SHA-256：`779B893DCBFDD0535D441AAB9BF3CEC7C83DCD99C3B5D76AFC2FF3B08D8B44D2`。

## 0.11.5 单一正式版收敛记录（2026-09-04）

- 已将最新版源码完整迁入 `D:\风月联机工具`，原 0.10.9 源码内容和独立镜像目录均已移除。
- 默认包名、appId、NSIS GUID、产品名、快捷方式及用户数据身份已统一为正式版；删除重复的镜像晋升配置与入口。
- 完成 171 项回归测试、TypeScript/Vite 构建、Electron 沙箱验证、打包清单与可执行文件元数据核验。
- 已静默覆盖安装正式版 0.11.5；正式用户数据保持存在，桌面快捷方式继续指向正式安装目录，卸载项与 ASAR 清单版本均为 0.11.5。
- 已移除旧镜像/晋升构建目录、`win-unpacked`、blockmap、构建调试文件和临时验证缓存，只保留当前正式安装程序。
- 正式安装包 SHA-256：`8054B2AB85E1ED2097A49CCAC158F56B487C3014EDDB86EFDFA7544B939A57F3`。
